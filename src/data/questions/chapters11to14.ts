import { defineQuestion as q } from './factory'

export const chapters11to14 = [
  q({ id:'ch11-q001',slug:'with-read-file',title:'用 with 读取文件',chapterId:'chapter-11',knowledgePointIds:['with-open'],type:'codeOrder',difficulty:'intro',
    description:'排列代码块，使用 UTF-8 文本模式读取 notes.txt 的全部内容并输出。',requirements:['必须使用 with 自动关闭文件','显式指定 utf-8','按唯一正确顺序排列'],starterCode:'',
    solutionCode:"with open('notes.txt', 'r', encoding='utf-8') as file:\n    content = file.read()\nprint(content)",codeBlocks:[{id:'open',code:"with open('notes.txt', 'r', encoding='utf-8') as file:"},{id:'read',code:'    content = file.read()'},{id:'print',code:'print(content)'}],correctCodeOrder:['open','read','print'],
    examples:[{id:'ch11-q001-e01',input:'',expectedOutput:'第一行\n第二行',visible:true,compareMode:'lineTrimmed',virtualFiles:[{path:'notes.txt',content:'第一行\n第二行',encoding:'utf-8'}]}],hiddenTests:[{id:'ch11-q001-h01',input:'',expectedOutput:'hello',visible:false,compareMode:'lineTrimmed',virtualFiles:[{path:'notes.txt',content:'hello',encoding:'utf-8'}]}],
    hints:['先打开文件才能读取。','read 必须缩进在 with 块内。','读取完成后再在块外 print。'],explanation:'with 建立上下文并在离开代码块时关闭文件；read 使用文件对象，所以必须位于打开之后。',estimatedMinutes:4}),
  q({ id:'ch11-q002',slug:'count-text-lines',title:'统计文本行数',chapterId:'chapter-11',knowledgePointIds:['text-file-reading'],type:'coding',difficulty:'basic',
    description:'读取固定虚拟文件 data.txt，输出文本行数。空文件为 0 行；末尾是否有换行不额外增加一行；空行本身计为一行。',requirements:['使用文本模式和 utf-8','不得访问其他路径','只输出非负整数'],
    starterCode:"with open('data.txt', 'r', encoding='utf-8') as file:\n    # 统计并输出",solutionCode:"with open('data.txt', 'r', encoding='utf-8') as file:\n    count = sum(1 for _ in file)\nprint(count)",
    examples:[{id:'ch11-q002-e01',input:'',expectedOutput:'3\n',visible:true,compareMode:'exact',virtualFiles:[{path:'data.txt',content:'a\nb\nc',encoding:'utf-8'}]}],hiddenTests:[{id:'ch11-q002-h01',input:'',expectedOutput:'0\n',visible:false,compareMode:'exact',virtualFiles:[{path:'data.txt',content:'',encoding:'utf-8'}]},{id:'ch11-q002-h02',input:'',expectedOutput:'3\n',visible:false,compareMode:'exact',virtualFiles:[{path:'data.txt',content:'a\n\nb\n',encoding:'utf-8'}]}],
    hints:['遍历文件对象会逐行产生文本。','每次迭代都把计数增加 1，包括空行。','可用 sum(1 for _ in file) 计数。'],explanation:'按文件对象逐行迭代直接遵循 Python 的行边界规则，不会把末尾换行误算为额外空行。',estimatedMinutes:6}),
  q({ id:'ch12-q001',slug:'valid-math-import',title:'判断 import 写法',chapterId:'chapter-12',knowledgePointIds:['imports'],type:'multipleChoice',difficulty:'intro',
    description:'哪段代码能从标准库 math 导入 sqrt，并直接调用 sqrt(9)？',requirements:['选择唯一语法和调用方式都正确的选项','只考虑 Python 标准库'],starterCode:'',solutionCode:'',
    choices:[{id:'a',text:'import sqrt from math\nsqrt(9)'},{id:'b',text:'from math import sqrt\nsqrt(9)'},{id:'c',text:'import math.sqrt\nsqrt(9)'},{id:'d',text:'from math import sqrt\nmath.sqrt(9)'}],correctChoiceIds:['b'],
    examples:[{id:'ch12-q001-e01',input:'b',expectedOutput:'b',visible:true,compareMode:'exact'}],hiddenTests:[{id:'ch12-q001-h01',input:'b',expectedOutput:'b',visible:false,compareMode:'exact'}],
    hints:['from 模块 import 名称 会把名称直接引入当前作用域。','直接调用 sqrt 要求当前作用域已有 sqrt。','正确形式是 from math import sqrt。'],explanation:'选项 b 的导入语法正确，且导入后可直接使用 sqrt；若只 import math，则应写 math.sqrt。',estimatedMinutes:3}),
  q({ id:'ch12-q002',slug:'circle-area',title:'计算圆面积',chapterId:'chapter-12',knowledgePointIds:['standard-library'],type:'fillBlank',difficulty:'basic',
    description:'补全代码，使用 math.pi 计算非负半径圆的面积并输出。',requirements:['必须通过 math 命名空间访问 pi','面积公式为 pi × r²','允许浮点误差 1e-9'],
    starterCode:"import math\nradius = float(input())\narea = ____.____ * radius ** 2\nprint(area)",solutionCode:"import math\nradius = float(input())\narea = math.pi * radius ** 2\nprint(area)",
    examples:[{id:'ch12-q002-e01',input:'0',expectedOutput:'0.0',visible:true,compareMode:'numeric',numericTolerance:1e-9}],hiddenTests:[{id:'ch12-q002-h01',input:'2',expectedOutput:'12.566370614359172',visible:false,compareMode:'numeric',numericTolerance:1e-9},{id:'ch12-q002-h02',input:'0.5',expectedOutput:'0.7853981633974483',visible:false,compareMode:'numeric',numericTolerance:1e-9}],
    hints:['import math 后模块名是 math。','圆周率常量名是 pi。','两个空依次填 math 和 pi。'],explanation:'模块导入不会自动把 pi 放入当前作用域，需要使用 math.pi；numeric 比较只放宽浮点表示误差。',estimatedMinutes:5}),
  q({ id:'ch13-q001',slug:'counter-class',title:'创建计数器类',chapterId:'chapter-13',knowledgePointIds:['class-basics'],type:'coding',difficulty:'basic',
    description:'实现 Counter：构造时 value 为 0；increment() 将 value 加 1 并返回新值。',requirements:['value 是实例属性','increment 使用 self','每次调用恰好增加 1'],
    starterCode:'class Counter:\n    def __init__(self):\n        pass\n\n    def increment(self):\n        pass',solutionCode:'class Counter:\n    def __init__(self):\n        self.value = 0\n\n    def increment(self):\n        self.value += 1\n        return self.value',testHarnessCode:"counter = Counter()\ncount = int(input())\nprint([counter.increment() for _ in range(count)])\nprint(counter.value)",
    examples:[{id:'ch13-q001-e01',input:'3',expectedOutput:'[1, 2, 3]\n3\n',visible:true,compareMode:'exact'}],hiddenTests:[{id:'ch13-q001-h01',input:'0',expectedOutput:'[]\n0\n',visible:false,compareMode:'exact'},{id:'ch13-q001-h02',input:'1',expectedOutput:'[1]\n1\n',visible:false,compareMode:'exact'}],
    hints:['实例属性写作 self.value。','在 __init__ 中把初值设为 0。','increment 先加 1，再 return self.value。'],explanation:'构造方法为每个实例建立 value；方法通过 self 更新当前对象并返回更新后的状态。',estimatedMinutes:7}),
  q({ id:'ch13-q002',slug:'fix-shared-list',title:'修复共享列表问题',chapterId:'chapter-13',knowledgePointIds:['instance-state'],type:'debug',difficulty:'skilled',
    description:'修复 Notebook，使每个实例拥有独立的 notes 列表；add(text) 只修改当前实例。',requirements:['不得保留可变类属性作为笔记容器','在 __init__ 创建实例列表','保留 add 方法接口'],
    starterCode:"class Notebook:\n    notes = []\n\n    def add(self, text):\n        self.notes.append(text)",solutionCode:"class Notebook:\n    def __init__(self):\n        self.notes = []\n\n    def add(self, text):\n        self.notes.append(text)",testHarnessCode:"first = Notebook()\nsecond = Notebook()\nfirst.add('A')\nsecond.add('B')\nprint(first.notes)\nprint(second.notes)\nprint(first.notes is second.notes)",
    examples:[{id:'ch13-q002-e01',input:'',expectedOutput:"['A']\n['B']\nFalse\n",visible:true,compareMode:'exact'}],hiddenTests:[{id:'ch13-q002-h01',input:'',expectedOutput:"['A']\n['B']\nFalse\n",visible:false,compareMode:'exact',explanation:'同时检查内容隔离和对象身份。'}],
    hints:['类属性由所有实例共享。','可变容器应在每次构造实例时新建。','添加 __init__ 并写 self.notes = []。'],explanation:'类体中的列表只创建一次；把列表创建移到 __init__ 后，每次实例化都会得到不同列表。',estimatedMinutes:8}),
  q({ id:'ch14-q001',slug:'grade-summary',title:'成绩单摘要',chapterId:'chapter-14',knowledgePointIds:['integrated-io-statistics'],type:'miniProject',difficulty:'combined',
    description:'实现 summarize(text)：text 是逗号分隔的非空整数成绩，范围 0–100。返回四行摘要：人数、最高分、最低分、平均分（两位小数）和及格人数（>=60）。',requirements:['函数接收字符串并返回摘要字符串','平均分固定两位小数','不得处理约束外非法输入'],
    starterCode:'def summarize(text):\n    # 返回固定格式摘要\n    pass',solutionCode:"def summarize(text):\n    scores = [int(item.strip()) for item in text.split(',')]\n    passed = sum(score >= 60 for score in scores)\n    return f'人数:{len(scores)}\\n最高:{max(scores)} 最低:{min(scores)}\\n平均:{sum(scores) / len(scores):.2f}\\n及格:{passed}'",testHarnessCode:'print(summarize(input()))',
    examples:[{id:'ch14-q001-e01',input:'59,60,100',expectedOutput:'人数:3\n最高:100 最低:59\n平均:73.00\n及格:2\n',visible:true,compareMode:'exact'}],hiddenTests:[{id:'ch14-q001-h01',input:'0,100',expectedOutput:'人数:2\n最高:100 最低:0\n平均:50.00\n及格:1\n',visible:false,compareMode:'exact'},{id:'ch14-q001-h02',input:'61,62,64',expectedOutput:'人数:3\n最高:64 最低:61\n平均:62.33\n及格:3\n',visible:false,compareMode:'exact'}],
    hints:['先 split 逗号并把各项转整数。','len、max、min、sum 可完成主体统计。','用 :.2f 固定平均分格式，再统计 score >= 60。'],explanation:'先建立一次成绩列表，再从同一数据源派生全部统计量，避免多次解析不一致；布尔值可参与 sum 统计及格数。',estimatedMinutes:15}),
  q({ id:'ch14-q002',slug:'text-cleaning-pipeline',title:'文本清洗流水线',chapterId:'chapter-14',knowledgePointIds:['text-processing-pipeline'],type:'miniProject',difficulty:'combined',
    description:'实现 word_frequencies(text)：将英文文本转小写，把逗号、句号、感叹号、问号替换为空格，统计单词，并按单词升序返回“word:count”逐行文本。无单词时返回空字符串。',requirements:['清洗顺序固定为小写、替换标点、切词','按单词升序','函数返回字符串而非打印'],
    starterCode:'def word_frequencies(text):\n    pass',solutionCode:"def word_frequencies(text):\n    cleaned = text.lower()\n    for mark in ',.!?':\n        cleaned = cleaned.replace(mark, ' ')\n    counts = {}\n    for word in cleaned.split():\n        counts[word] = counts.get(word, 0) + 1\n    return '\\n'.join(f'{word}:{counts[word]}' for word in sorted(counts))",testHarnessCode:'print(word_frequencies(input()))',
    examples:[{id:'ch14-q002-e01',input:'Hello, hello world!',expectedOutput:'hello:2\nworld:1\n',visible:true,compareMode:'exact'}],hiddenTests:[{id:'ch14-q002-h01',input:'\n',expectedOutput:'\n',visible:false,compareMode:'exact'},{id:'ch14-q002-h02',input:'B? a. b, A!',expectedOutput:'a:2\nb:2\n',visible:false,compareMode:'exact'}],
    hints:['先 lower，再循环替换四种标点。','用字典的 get 累加词频。','sorted(counts) 后用换行符 join。'],explanation:'先把标点变为空格可避免相邻单词粘连；split 自动忽略连续空白，排序保证输出稳定可判定。',estimatedMinutes:16}),
  q({ id:'ch14-q003',slug:'menu-output',title:'菜单程序输出预测',chapterId:'chapter-14',knowledgePointIds:['while-menu'],type:'output',difficulty:'basic',
    description:'阅读使用固定命令迭代器的程序，写出精确输出。next(inputs) 依次取得 a、x、q，本题不读取标准输入。',requirements:['inputs 的值按给定顺序消费','遇到 q 后终止','每行内容和顺序必须精确'],starterCode:'',solutionCode:'',promptCode:"inputs = iter(['a', 'x', 'q'])\nwhile True:\n    command = next(inputs)\n    if command == 'q':\n        print('bye')\n        break\n    elif command == 'a':\n        print('add')\n    else:\n        print('unknown')",
    examples:[{id:'ch14-q003-e01',input:'',expectedOutput:'add\nunknown\nbye',visible:true,compareMode:'exact'}],hiddenTests:[{id:'ch14-q003-h01',input:'',expectedOutput:'add\nunknown\nbye',visible:false,compareMode:'exact'}],
    hints:['按 inputs 列表从左到右跟踪 command。','a 和其他字符进入不同分支。','q 先输出 bye，再 break。'],explanation:'循环依次处理 a、x、q，分别输出 add、unknown、bye；break 使迭代器耗尽前正常终止。',estimatedMinutes:5}),
  q({ id:'ch14-q004',slug:'contact-query',title:'联系人查询器',chapterId:'chapter-14',knowledgePointIds:['dictionary-application'],type:'miniProject',difficulty:'basic',
    description:'实现命令行联系人查询器。每行命令为“add 姓名 电话”“find 姓名”或“quit”。add 新增或覆盖并输出 saved；find 输出电话或 not found；quit 输出 bye 并结束。',requirements:['命令以单个或多个空白分隔','重复姓名由新电话覆盖','每条命令只输出一行'],
    starterCode:'contacts = {}\n# 循环读取并处理命令',solutionCode:"contacts = {}\nwhile True:\n    parts = input().split()\n    command = parts[0]\n    if command == 'quit':\n        print('bye')\n        break\n    if command == 'add':\n        contacts[parts[1]] = parts[2]\n        print('saved')\n    elif command == 'find':\n        print(contacts.get(parts[1], 'not found'))",
    examples:[{id:'ch14-q004-e01',input:'add Alice 100\nfind Alice\nquit',expectedOutput:'saved\n100\nbye\n',visible:true,compareMode:'exact'}],hiddenTests:[{id:'ch14-q004-h01',input:'find Bob\nquit',expectedOutput:'not found\nbye\n',visible:false,compareMode:'exact'},{id:'ch14-q004-h02',input:'add A 1\nadd A 2\nfind A\nquit',expectedOutput:'saved\nsaved\n2\nbye\n',visible:false,compareMode:'exact'}],
    hints:['用字典保存姓名到电话的映射。','add 直接赋值即可同时支持新增和覆盖。','find 用 get(name, "not found")，quit 时 break。'],explanation:'字典赋值天然覆盖同名联系人；get 提供缺失时结果。quit 分支在读取其他参数前结束，避免索引错误。',estimatedMinutes:10}),
] as const
